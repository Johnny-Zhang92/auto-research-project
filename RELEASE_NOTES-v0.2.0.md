# OpenCode 科研模式 0.2.0 发行说明

0.2.0 是第一个面向真实实验执行的安全加固版。它保留 0.1 已验证的自动四阶段推进能力，同时修复了“只验证文件存在”“一次批准后即可大量调用”“脚本命令可绕过权限”“并发或中断状态不明确”等问题。

## 已验证

- 新项目连续推进至计划审批门；
- 批准后只执行实现与 smoke，并停在第二审批门；
- 二次批准、冻结、正式实验、分析、审查和报告完整闭环；
- 无效结构化产物在两次尝试后进入 `blocked`；
- 预计成本超过批准预算时拒绝二次批准；
- 冻结后修改输入会在正式实验前阻断；
- stale-running 恢复和同项目并发锁；
- Guard 正常完成与成本超限终止；
- Agent 权限和 Workflow 静态策略。

## 尚未替你在真实环境验证

- 真实 DeepSeek V4 Pro/Flash API 调用与账单对账；
- OpenCode 1.18.27 在你的主机上从 0.2 新项目跑到第二审批门；
- SSH/Slurm 远程实验。

因此推荐先用小预算创建 `deepseek-benchmark-v02`，只跑到第二审批门。审阅 manifest、任务和 smoke 产物后，再决定是否正式执行。

## 已知限制

- OpenCode 权限不是操作系统容器沙箱。
- 成本 Guard 依赖实验脚本在每个会话结束后更新 `progress.json`；单次已发出的 API 请求无法撤回。
- 小型 benchmark 只适合验证 Harness，不足以支持普遍性的模型优劣结论。
- 0.1 项目不能原地升级，见 `MIGRATION.md`。
